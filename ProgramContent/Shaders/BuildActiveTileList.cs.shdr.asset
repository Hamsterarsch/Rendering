{
	`code`: `cbuffer localConstantBuffer : register(b0) { uint tileCount; }
Buffer<uint> volumeTileFlags : register(t0);
RWBuffer<uint> activeTileList : register(u0);

[numthreads(32, 1, 1)]
void main(uint3 dispatchID : SV_DispatchThreadID)
{
if(dispatchID.x >= tileCount)
{
return;

}

if(volumeTileFlags[dispatchID.x])
{
uint writeIndex;
InterlockedAdd(activeTileList[0], 1, writeIndex);
activeTileList[writeIndex+1] = dispatchID.x;
}

}
`,
	`numConstantsSlots`: `0`,
	`numTextureSlots`: `0`
}