{
	`code`: `#include "Types/GlobalBufferData.hlsl"

StructuredBuffer<float4x4> transforms : register(t0);
cbuffer buffer : register(b1) { uint transformOffset; };

float4 main(float3 pos : POSITION, uint instanceID : SV_InstanceID) : SV_POSITION
{
return  mul(projection, mul(view, mul(transforms[transformOffset+instanceID], float4(pos.xyz, 1))));

}

`,
	`numConstantsSlots`: `0`,
	`numTextureSlots`: `0`
}