cbuffer globals : register(b0)
{
	float4x4 view;
	float4x4 projection;
	float time;
};


float4 main(float4 pos : SV_POSITION) : SV_TARGET
{
    float color = sin(time * 0.01) * 0.5 + 0.5;
	return float4(color,color,color,1);

}
